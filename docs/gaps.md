# Gaps and next milestone

## This slice does not include

- Workspace, hotseat, folders, widgets, shortcuts, drag-and-drop
- Recents / Quickstep (privileged; do not claim)
- Private Space chrome, app lock, launcher data provider
- Icon packs / Cuscon
- TalkBack certification
- mustang / GrapheneOS device observation (fingerprint, live grid)
- Room workspace snapshot (metadata-only v1)
- Compile SDK 17 (nixpkgs androidenv is 35)

## Next milestone

Workspace + phone grid on public APIs (`INSTALLABLE_PUBLIC`), still no Recents. Add Room tables only with that workspace. Attach a mustang before claiming device grid/fingerprint.
