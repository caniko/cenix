# 0006. Emergency mode

If JNI is missing, the native filter errors, Room cannot open, or three startups fail in 60s, Cenix stays up and filters in Kotlin. Retry native and reset local state are explicit user actions.
